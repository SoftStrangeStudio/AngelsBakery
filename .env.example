# Public build-time settings. NEVER put private credentials in NEXT_PUBLIC_*.
NEXT_PUBLIC_ORDER_ENDPOINT=
# Keep false until approved menu, pickup details and real-browser receipt test pass.
NEXT_PUBLIC_ORDERING_ENABLED=false
